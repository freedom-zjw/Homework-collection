dealData.py 为数据集处理，请使用py3运行。
现提供处理好的数据集，已和运行代码匹配。

66.csv为最优结果


如果TA条件允许请使用GPU，也就是带有GPU的  GPUregression.m
如果没有条件可以选择使用CPU也就是不带GPU的 regression.m


两个Seed文件为CPU和GPU运行时的w和b的种子，也就是最优的初始w和b


上述两种运行方式在加载种子之后

GPU版能符合2018/1/2 rank_all 上的最优值。
CPU版会因为精度计算方式和GPU不同导致不是完全符合的值，会有所偏差但是不大。

结果输出为66_v1.csv

两种代码只在MATLAB2016a的以上版本运行，不保证在在低版本可用，低版本可能会出现不支持一个矩阵 + 一个列的操作。

如果代码无法运行可以联系我们进行远程桌面的操作or实验课当场运行。